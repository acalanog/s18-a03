# Q1: How many employees does the city have?
import helper

data = helper.read_salaries()# will read the data
city_employees = len(data) #counts length of data
print(city_employees)

#33,183 city_employees
